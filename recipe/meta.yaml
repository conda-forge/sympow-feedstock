{% set name = "sympow" %}
{% set version = "2.023.4" %}
{% set sha256 = "bc7985ee8dce5e6dc129365280ec2d4ae8475383c24d40bf07b3016ad62cba70" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://salsa.debian.org/science-team/sympow/-/archive/upstream/{{ version }}/sympow-upstream-{{ version }}.tar.gz
  sha256: {{ sha256 }}
  patches:
    - patches/0001-OSX-apparently-does-not-know-about-t.patch

build:
  number: 0
  skip: true  # [win]
  ignore_run_exports:
    - pari

requirements:
  build:
    - {{ compiler('c') }}
    - libtool
    - autoconf
    - automake
    - help2man
    - make
  host:
    - pari
  run:
    # new_data invokes PARI's GP
    - pari

test:
  commands:
    - sympow -help
    - sympow -new_data 2

about:
  home: http://files.sagemath.org/spkg/upstream/sympow/index.html
  license: BSD-variant
  license_family: BSD
  license_file: {{ SRC_DIR }}/COPYING
  summary: A package to compute special values of symmetric power elliptic curve L-functions

extra:
  recipe-maintainers:
    - isuruf
    - saraedum
